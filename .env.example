# 토스 콘솔에서 발급받은 API 키 (실제 값은 .env 에)
TOSS_API_KEY=

# 텔레메트리 엔드포인트 (F09 - D1 SDK 조사 결과에 따라 결정)
# VITE_TELEMETRY_ENDPOINT=https://coffilens-telemetry.workers.dev/track

# **F09 Phase 2 — 광고 그룹 ID** (토스 콘솔 발급)
# Home/Result 화면 banner 의 adGroupId. 미설정 시 banner 미노출 (fail-soft).
VITE_AD_GROUP_HOME=
VITE_AD_GROUP_RESULT=
